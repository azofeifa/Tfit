#ifndef template_H
#define template_H
#include "load.h"
#include <math.h>
#include <vector>
vector<double> peak_bidirs(segment * );
int sample_centers(vector<double>, double);
extern double INF;
extern double  nINF;

#endif
